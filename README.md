# News Site client side

using: `reactjs` and `bootstrap`\
server side: [github.com/beso9595/news-site-server](https://github.com/beso9595/news-site-server)

* `git clone`
* `npm start`
* deployed to `localhost:3000`